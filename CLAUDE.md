# Claude Code entry — see AGENTS.md

Read `AGENTS.md` for skill routing. Primary workflows use `.agents/skills/`.
Development path is remote-code-parity + shared mount root hostPath, not image
rebuild per edit.
