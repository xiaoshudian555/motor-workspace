interface:
  display_name: "Motor Workspace Deploy"
  short_description: "Route Motor deploy, wheel, feasibility, and reliability work"
  default_prompt: "Use $motor-deploy to route this Motor workspace deployment, wheel replacement, or feasibility request and enforce unsupported reliability boundaries."
policy:
  allow_implicit_invocation: true
