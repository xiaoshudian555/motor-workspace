interface:
  display_name: "Motor Functional Validation"
  short_description: "Run focused Motor functional checks with direct tools"
  default_prompt: "Use $motor-functional to select minimal cases and validate them against current Motor services."
