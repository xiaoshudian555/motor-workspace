"""Claude Code adapter notes for remote-dev.

Tool names may be displayed by clients as sanitized variants such as
`mcp__remote-dev__remote_read`; the server accepts both `remote.read` and
`remote_read` call names.
"""
